# TVMIX Frontend

Frontend OTT realizzato con Next.js, TypeScript, Tailwind CSS e hls.js.

```bash
npm install
npm run dev
```

Il player demo usa lo stream HLS pubblico di Mux. Sostituire la prop `src` in
`components/PlayerModal.tsx` con l'endpoint `.m3u8` di produzione.
